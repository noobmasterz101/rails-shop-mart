<h1>SHOP MART</h1>

<% if Current.user %>
    Logged in as: <%= Current.user.email %><br>
    <%= link_to 'Edit Password', edit_password_path %>
    <%= button_to 'Logout', logout_path, method: :delete %>
<% else %>
    <p><%= link_to 'new user? register now', sign_up_path %></p>
    <p><%= link_to 'user login', sign_in_path %></p>
<% end %>

    <% if Current.admin %>
        Logged in as: <%= Current.admin.email %><br>
        <%= link_to 'Edit Password', ad_edit_password_path %>
        <%= button_to 'Logout', ad_logout_path, method: :delete %>
    <% else %>
        <p><%= link_to 'Admin Sign Up', ad_sign_up_path %></p>
        <p><%= link_to 'Admin Login', ad_sign_in_path %></p>
    <% end %>